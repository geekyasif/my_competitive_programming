#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    string str = "abec";
    for (int i = 0; i < str.length(); i++)
    {
        for (int j = 0; j < str.length(); j++)
        {
            if (str[j] == 'a' || str[j] == 'e' || str[j] == 'i' || str[j] == 'o' || str[j] == 'u')
            {
            }
            else
            {
            }
        }
    }
    return 0;
}